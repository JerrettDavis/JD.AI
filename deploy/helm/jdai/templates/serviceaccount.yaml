{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jdai.serviceAccountName" . }}
  labels:
    {{- include "jdai.labels" . | nindent 4 }}
{{- end }}
